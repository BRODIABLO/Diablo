# ISC12 — ItemStatCost 12-bit clean-sheet format

Dernière mise à jour : 30 août 2026

## Décision produit

Vincent autorise le 29 août 2026 l'implantation d'**ISC12** et choisit le
séquencement **A — sécurité d'abord**. ISC12 devient une DLL autonome attribuée
exactement à **RuffnecKk**, membre de la RuffnecKk D2RLoader Suite, et ne sera
jamais fusionnée dans une DLL d'eezstreet.

Vincent retient le 30 août 2026 le produit réseau **G9-A native-only**. ISC12
reste autonome, utilise exclusivement les paquets natifs `0x9C`/`0x9D` et
conserve les plafonds exacts : 244 octets pour un root `0x9C` et
239 octets pour chaque nœud `0x9D`. La transaction source préparée préflighte
l'arbre complet avant toute queue réelle et refuse l'envoi entier si un seul
nœud, compte, liste ou lien de topologie est invalide; ce trajet n'a pas encore
été exécuté en runtime. La coexistence avec les cinq plugins
officiels d'eezstreet reste une matrice de qualification, jamais une dépendance
fonctionnelle ni une capacité de transport ISC12.

Le produit étend le namespace sérialisé d'`ItemStatCost` de 9 à 12 bits :

- IDs valides : `0..4094`;
- sentinelle de fin de liste bitstream : `4095` (`0xFFF`);
- capacité maximale : 4095 définitions de statistiques;
- description publique candidate : `Supports up to 4,095 item stat definitions for overhaul mods.`

La DLL est hybride globale/mod-locale, sans `ModScopedOnly`, avec sa propre
version, ses métadonnées et une configuration TOML indépendante de
`D2RPlugins.json`. Le contenu et les commentaires du TOML restent en anglais.
Une configuration présente mais invalide doit refuser le chargement avant tout
hook; son absence utilise uniquement les valeurs sûres documentées.

## Contrat clean-sheet

ISC12 ne vise aucune compatibilité directe avec les sauvegardes vanilla ou les
anciens flux 9 bits. Une partie ISC12 doit être créée ou migrée explicitement
vers le nouveau format. Les sauvegardes ISC12 restent liées à la présence du
plugin et à un schéma `ItemStatCost` compatible.

Le format doit contenir, avant tout décodage de statistiques :

1. un namespace ou magic propriétaire ISC12;
2. une version de format ISC12;
3. une empreinte déterministe de l'ordre et des IDs de la table
   `ItemStatCost`;
4. les informations requises pour recalculer taille et checksum de
   l'enveloppe.

L'absence ou la non-concordance de ce contrat refuse proprement le chargement
avant `ITEMS_DecodeItemFromBitstream` et avant les lecteurs de stats joueur.
Changer uniquement la version D2S n'est pas accepté comme marqueur : le loader
natif tolère des versions futures et les blobs d'items ne sont pas
auto-descriptifs. Le store autonome du shared stash doit recevoir un marqueur
équivalent après identification statique et runtime de son enveloppe exacte.

Un outil externe de migration, potentiellement maintenu par Killshot, reste un
produit séparé : il décode une sauvegarde 9 bits puis réémet intégralement le
format ISC12. Aucune migration in-place n'est revendiquée.

## Séquencement retenu — sécurité d'abord

1. Mission, ROADMAP, workstream et contrat autonome Suite.
2. Manifeste natif exhaustif et empreinte fail-closed avant le premier hook.
3. Squelette DLL inerte, configuration et tests négatifs de l'empreinte.
4. Extension du loader `ItemStatCost` et remplacement borné de la liste
   temporaire `DescFunc` par un stockage de 4 095 entrées, sans codec ISC12
   actif.
5. Marker clean-sheet, fingerprint de table et refus des stores non ISC12.
6. Codecs item/joueur/preview 12 bits et round-trip sur fixtures jetables.
7. Codecs réseau, estimateurs de taille et guards d'overflow.
8. Qualification statique, runtime, gameplay et coexistence complète.

Le sous-gate `G0-BBE` traverse ce séquencement en **sécurité d'abord** : sa
preuve statique commence hors runtime en parallèle de la décision G9 et bloque
toute publication native tant qu'elle n'est pas fermée. Son témoin en jeu reste
différé jusqu'à l'ouverture des gates runtime existants.

**Gate absolu :** aucun binaire capable d'écrire un flux ISC12 ne sera déployé
contre une vraie sauvegarde tant que le marqueur clean-sheet, le refus vanilla,
le fingerprint de table, les guards de capacité et des fixtures jetables ne
sont pas verts.

## Faits natifs vérifiés

Le corpus gouverné commun aux builds D2R `3.2.92777` et `3.3.93847` est prêt et
byte-exact sur les surfaces inspectées. Le runtime réellement visé pour la
qualification complète est D2R `3.3.93847`; `3.2.92777` ne sera déclaré couvert
par équivalence que si chaque surface finale demeure identique.

### Table et runtime

- `DATATBLS_CompileItemStatCostTxt` : `RVA 0x31E310`.
- Compilation TXT générique : appel `0x31EC89`, stride `0x144`.
- Vecteur des records : data `DataTables+0x1258`, size `+0x1260`, capacity
  `+0x1268`; vecteur final des IDs `DescFunc` à `+0x1278`, avec ABI
  `{uint16_t* data; uint64_t size; uint64_t capacityAndFlags}` et masque de
  capacité `0x7FFFFFFFFFFFFFFF`.
- Cap vanilla explicite : `0x31ED31`, compare du count à `0x1FF`.
- Accesseur `ItemStatCost` : `0x2D9730`, ID dword et count 64 bits.
- Clés de statlist : `(uint16(statId) << 16) | uint16(layer)`.
- Les références internes restent des words avec sentinelle interne `0xFFFF`;
  cette sentinelle ne doit jamais devenir `0xFFF`.

Le bloc `0x31F0AB..0x31F1B3` utilise une zone fixe de 512 entrées
`{uint16_t statId, int16_t priority}`. Le jalon 0.2.0 détourne la couture exacte
vers un stockage borné de 4 095 entrées, réutilise le qsort, le comparateur signé
et le resizer natifs, puis revient à l'épilogue avec les registres requis
restaurés. Modifier seulement le cap du loader demeure interdit.

### BBE `stat()` — native width and membership closed

Vincent retient le 30 août 2026 l'option 1 « sécurité d'abord ». Ce contrôle ne
crée ni nouveau plugin ni nouvelle mutation : il ferme une surface de
compatibilité du namespace ISC12 révélée par les formules BBE
`stat('name'.accr)`.

Ce sous-gate ISC12 est distinct de la fonctionnalité officielle eezstreet
`items.playerConditionCalc`. Cette dernière reste une surface de coexistence du
PluginPack; elle ne prouve ni la largeur de l'ordinal BBE natif ni sa conservation
jusqu'à l'évaluateur et ne peut donc pas fermer G0-BBE.

Le census D2R 3.3 gouverné trouve sept callsites de compilation, trois frontends
qui convergent vers un seul resolver core, et sept callsites d'évaluation D2R
câblés à la table protégée; un huitième appel générique passe une table et un
count nuls. La
branche `0x3B5D80` consulte le linker `ItemStatCost` à `DataTables+0x1270` et
stocke l'ordinal comme DWORD. Le compilateur générique émet token 5 + WORD pour
les valeurs signées 16 bits; tous les IDs ISC12 `512..4094` y tiennent. Le
decoder retransmet ce WORD en EDX et le callback stat `0x3B33F0` garde le
DWORD complet jusqu'aux trois accesseurs de stat. Aucun masque `0x1FF`, store
étroit ou conflit avec la sentinelle ISC12 n'est présent sur ce code path.

**Owner : AGENT — statut : native width and membership closed.**
Le ledger gouverne maintenant dix sites G0-BBE proof-only et son validateur
interdit toute mutation dans ce groupe. Une capture mémoire gouvernée du
runtime officiel D2R `3.3.93847`, réalisée le 31 août 2026 avec seulement
`PROCESS_QUERY_LIMITED_INFORMATION | PROCESS_VM_READ`, ferme les deux arêtes
que le corpus statique protégé laissait opaques. Les ancres `.text`
`0x3B59CA = 488D15BB4C9401` et `0x3B54A3 = 488D05A6479501` correspondent
byte-exactement. Le littéral `0x1CFA68C` vaut `stat\0` et son témoin de 16
octets a le SHA-256
`83626D991B1A0AD789DDB50D993623989AB1C1257879530005CEE0CA411F0E4F`.

La même lecture corrige le layout attendu à `0x1D09C50` : il s'agit de neuf
records de 16 octets `{callback VA:qword, arity:dword, padding:dword}`, donc
144 octets et non 72. Le record du slot 5 à `0x1D09CA0` contient le callback
RVA `0x3B33F0`, arity `2`, padding zéro. Le hash SHA-256 ASLR-normalisé de la
table complète est
`E8B9B76F9D7320BDFA8129F8D22B0CB19B450AC4D655F53A8D2E56E47CF224ED`.
Aucune écriture, suspension, injection, sauvegarde ou action gameplay n'a eu
lieu. La capture post-startup atteste le runtime actif; un watcher pré-TXT puis
post-TXT pourra renforcer séparément la preuve d'immuabilité de lifecycle.
D2MOO reste seulement une corroboration sémantique. La fixture jetable `512`
puis `4094` appartient toujours à la qualification runtime ultérieure et ne
touchera aucune vraie sauvegarde.

### Codecs de sauvegarde

- Codec item générique G1 : les lecteurs conservent leurs mutations
  width/sentinelle/seed, dont `previousStatId = -1` à `0x37AB36`. L'ancien
  patch writer limité à `0x37F186` est invalidé : l'owner possède une table
  de suppression compound de 511 DWORD et le lookup à `0x37F17C` lirait hors
  table pour tout ID `>=511`.
- Solution G1 retenue : remplacer in-place le corps exact de 42 octets
  `[0x37F17C,0x37F1A6)`, après le guard non-zéro inchangé à `0x37F174`.
  Les IDs `0..510` gardent la comparaison et la suppression compound; les IDs
  supérieurs contournent le lookup; le writer émet `min(ID,0xFFF)` sur 12 bits
  avec le CALL natif `0x37F1A1` inchangé. L'ID réel `0xFFF` reste refusé par le
  schéma. Cette fenêtre remplace l'ancien site, sans double ownership.
- Wrapper de décodage item : `0x374BF0`.
- Wrapper de sérialisation item : `0x375EE0`, core `0x37FFD0`.
- Stats joueur modernes : callers exhaustifs `0x52EC4A/0x530A34`, owner intact
  `0x533760`, lecteurs `0x533968/0x533A9D`, écrivain `0x535303`, sentinelle
  `0x5353B8`; les épilogues natifs utilisent le statut malformé `0x12`.
- Sous-flux auxiliaire moderne : caller exhaustif `0x531A6D`, owner intact
  `0x530A00`, lecteurs `0x530AA3/0x530BAD`, écrivain `0x5340CD`, sentinelle
  `0x534149`.
- Le parseur frontend du D2S possède quatre lecteurs 9 bits autour de
  `0x61D251`, `0x61D29A`, `0x61D651` et `0x61D69A`. L'exact v105 atteint
  seulement les deux derniers (branche B); le CALL de copie gardable est
  `0x61CF90` vers l'owner partagé intact `0xA1E110`.

Les lectures de quantité item à `0x37A67F/0x37A6A6`, les `State ID`, les
`SaveBits`, `SaveParamBits`, `CsvBits` et `CsvParam` ne sont pas des IDs ISC et
restent inchangés.

### Réseau

- `0x3E` stat unique : sûr après conversion symétrique, `+3` bits.
- `0xA8` liste de stats : sûr, maximum 16 stats, jusqu'à 51 bits ajoutés.
- `0xAA` sync groupée : fragmentation existante entre States; l'estimateur aux
  sites `0x539EE7` et `0x539F78` doit compter 12 bits. Les `State ID` externes
  et leur sentinelle restent sur 9 bits.
- `0xAC` : jusqu'à 51 bits ajoutés, aucune fragmentation; headroom runtime à
  mesurer.
- `0x9C/0x9D` : chaque producer sérialise un seul nœud avec récursion désactivée.
  Le root utilise `0x9C` ou `0x9D`, puis chaque descendant socketé est un nouveau
  `0x9D`. Les caps natifs sûrs sont 244 octets pour le root `0x9C` et 239 pour
  chaque `0x9D`; tout l'arbre doit être préflighté avant le premier envoi. La
  preuve native borne localement chaque item à `occupied <= 7`, mais aucune
  borne native globale du nombre total de nœuds ou de la profondeur de l'arbre
  récursif n'est établie. Le staging ISC12 impose donc ses caps propres : 64
  paquets, `0x4000` octets, profondeur 16 et sept enfants immédiats; il copie les
  vrais octets et valide le batch entier avant tout replay par `0x4817F0`.

Tous les pairs d'une session doivent charger ISC12 et présenter la même
empreinte de table. Aucun support Battle.net officiel n'est revendiqué.

## Performance et limites distinctes

La table principale maximale occupe environ 1,27 Mio par contexte et ne pose
pas de problème structurel observé. En revanche, la génération de tooltip
balaye la liste `DescFunc` : BKVince possède actuellement 394 lignes ISC, dont
241 descriptibles. Une table remplie de 4095 stats descriptibles pourrait
multiplier ce scan par environ 17. Des benchmarks sparse et dense sont requis.

ISC12 étend la valeur maximale d'un ID; il ne promet pas des milliers de stats
simultanées sur un seul item ou une seule unité. Les snapshots natifs bornés à
511, 512 ou 1022 entrées restent un contrat séparé.

## Baseline et coexistence

- Runtime à qualifier : D2R `3.3.93847`.
- Build couvert seulement par équivalence native complète : `3.2.92777`.
- D2RLoader/API-SDK : baseline gouvernée Suite à relever avant compilation.
- Référence PluginPack : eezstreet D2RL-Plugins commit
  `dc75b49ffbb67b887d7757ee00ee9a03bcde5d8a`.
- Référence sémantique seulement : D2MOO commit
  `19019806df7f3e877fa105b05395d1e3597e2316`.

Le README public créditera explicitement D2MOO pour la compréhension
sémantique du compilateur `ItemStatCost`. Aucune adresse, structure ou ABI
D2MOO 32 bits n'est transposée dans le plugin.

Le clone officiel eezstreet épinglé au commit ci-dessus ne contient ni
ExtendedItemStats, ni EIT1, ni les six hooks de transport historiquement
audités. Ceux-ci proviennent du prototype RuffnecKk ExtendedItemStats, ensuite
exercé dans un build du fork expérimental
`RuffDood/D2RL-Plugins:codex/pluginpack-foundation`; aucune acceptation upstream
eezstreet n'est revendiquée. Cette piste reste une preuve historique neutre,
hors produit ISC12. G9-A n'en dépend pas et ne négocie aucun contrat de
transport externe.

Le scan des sources Suite et des cinq sources eezstreet épinglées n'a trouvé
aucun chevauchement de byte range avec les lecteurs, writers, sentinelles et
estimateurs internes ISC12 déjà inventoriés. Le témoin `0x12E4B0` reste une
fenêtre native exacte de budget/buffer, sans attribution de son entrée au
PluginPack officiel. Le dernier gate de coexistence demeure le scan des DLL
réellement installées et la matrice complète avec les cinq plugins officiels
eezstreet, Gear Swap Inventory, Remote Stash et MassID; cette matrice ne fournit
aucune capacité de transport à ISC12.

## Approche implantée

L'option retenue est un patch chirurgical :

- conserver le compilateur TXT, les structures et les statlists natives;
- étendre le count autorisé à `0xFFF`;
- remplacer seulement le tail temporaire `DescFunc` par un helper borné à
  4 095 entrées;
- patcher uniquement les champs prouvés comme `ItemStatCost ID`;
- apparier chaque lecteur avec son écrivain et sa sentinelle;
- ajouter les estimateurs, préflights et refus d'overflow correspondants;
- refuser toute empreinte native, sauvegarde ou session incompatible avant le
  premier hook ou décodage.

Une réécriture complète du loader et un namespace sidecar avec traduction sont
explicitement rejetés : ils augmenteraient la surface ABI, la dualité
d'identité et le risque de régression sans gain démontré.

## Matrice de validation requise

### Statique et tests purs

- empreinte positive du corpus gouverné et mutations négatives de chaque
  signature;
- absence de gate par build-name ou version D2R;
- manifests et trois exports D2RLoader attendus;
- fixtures de 512, 1023, 2047 et 4095 lignes;
- IDs frontière `510`, `511`, `512`, `1022`, `1023`, `2047`, `4094`;
- `DescFunc` sparse et dense, priorités triées, op stats vers IDs élevés;
- conservation de la sentinelle interne `0xFFFF` et sentinelle bitstream
  `0xFFF`;
- configuration absente, valide et invalide;
- mesure exacte `3 * (IDs + sentinelles)` des bits supplémentaires;
- préflight des capacités save et réseau.

### Sauvegardes jetables

- création ISC12 neuve, save/reload et deux cycles byte-stables;
- refus d'un D2S vanilla avant tout codec;
- refus d'une empreinte de table différente;
- inventaire, équipement, Cube, personal stash, corpse, mercenaire, Iron Golem
  et objets socketed;
- shared stash uniquement après preuve de son enveloppe et de son marker;
- corruption de marker, version, taille et checksum;
- aucune vraie sauvegarde utilisateur pendant l'incubation.

### Runtime et coexistence

- build Release x64 `/W4 /WX`, tests et reproductibilité;
- portées mod-locale et globale avec repli de configuration;
- pile complète active, tous les composants RuffnecKk compatibles et les cinq
  DLL eezstreet, aucune fonctionnalité neutralisée;
- deux ordres de chargement pour toute surface partagée;
- paquets `0x3E`, `0xA8`, `0xAA`, `0xAC`, `0x9C`, `0x9D`;
- mesure du headroom réel `0xAC/0x9C/0x9D` et refus propre des dépassements;
- solo, hôte/joiner avec plugin et table identiques, puis mismatches refusés;
- benchmark tooltip `DescFunc` sparse/dense;
- cold start, création, mort/corpse, mercenaire, save & exit et rechargement.

## Rollback

Le rollback binaire consiste à retirer ISC12 et restaurer la pile Suite
précédemment hashée. Il ne rend pas les sauvegardes ISC12 compatibles vanilla.
Les fixtures et personnages ISC12 utilisent un namespace séparé et des backups
hors du profil utilisateur réel. Le seul rollback de données admis est une
conversion externe explicitement prouvée; elle n'est pas promise par la DLL.

## Frontière Git active

Le lot ISC12 couvre :

- `Mission/isc12-3.3.md`;
- `addons/ISC12/**`;
- les promotions natives strictement nécessaires dans
  `reverse-engineering/d2r-3.2.92777/known-rvas.json` et `findings.md`;
- les scripts de preuve nommés `scripts/reverse-engineering/isc12*`;
- la configuration et la DLL runtime ISC12 seulement lorsqu'un gate de
  déploiement l'autorise;
- la ROADMAP, `Mission/CURRENT.md`, `Mission/WORKSTREAMS.json` et le cadastre en
  tant que fichiers partagés modifiés chirurgicalement.

Tous les autres changements du workspace appartiennent à leurs chantiers
existants et restent intacts. Le GO ISC12 n'autorise aucun commit, push, tag,
GitHub Release ni migration de vraie sauvegarde.

## Prochain gate

ISC12 `0.2.0` implante le loader G0 derrière un relais RX persistant et un état
RW séparé, avec stockage `DescFunc` borné à 4 095 entrées. G1 et G9-A sont
fermés en source et statiquement : 24 sites, 102 mutations, 77 témoins et zéro
publication; aucun caller production de Commit, `itemTransportReady == 0` et
`codecReady == 0`. Release `/W4 /WX`, CTest `4/4`, l'unicité des signatures
ajoutées et le ledger gouverné `VALID` à 211 sites / 15 groupes passent. La
configuration distribuée et le fallback embarqué sont désactivés par défaut;
les codecs item, sauvegarde et réseau restent 9 bits dans le processus.
**Publication/cold start ISC12, sauvegarde réelle et multijoueur NOT RUN; seule
l'attestation externe G0-BBE QUERY/READ a inspecté le runtime déjà ouvert.**

G10-A « file-envelope seams » est fermé statiquement. Le loader D2S, le sérialiseur
D2S avec sa taille/checksum finale, les deux readers et builders sectoriels D2I,
la collection de blobs puis sa fragmentation serveur en messages `0x00B3` sont
maintenant identifiés. Le handler client `0x00B3`, son réassemblage, le
finaliseur whole-D2S et la copie du buffer complet vers l'objet en état `4` sont
également prouvés. Le store secondaire est un D2I physique concaténé unique;
son reader whole-file et son splitter d'upload sont prouvés avant les codecs
sectoriels. Le lifetime du buffer unwrap D2S est fermé.

Le writer physique commun `0x9F94A0` est prouvé non atomique : il ouvre le
chemin final avec `CREATE_ALWAYS`, effectue un seul write dont il ignore statut
et longueur, puis déclare l'objet sauvegardé. G10-B P1/P2 est maintenant fermé
hors-jeu : primitive sibling-temp/full-write/flush/replace/rollback, enveloppe
v1 de 96 octets, préflight D2S/D2I, descripteur canonique sémantique et golden
vectors compilent sous `/W4 /WX` et passent CTest. Le descripteur lie l'ordinal
physique au `Stat` UTF-8 exact, inclut seulement les champs save courants v105
et ignore `*ID`, déjà incohérent aux lignes physiques 212/213 de BKVince. Les
trois champs legacy sont exclus après preuve que le sélecteur autoritaire dépend
de la version du conteneur, strictement 105, et non du tag individuel de l'item.
Le golden hash v1 est
`37EB7F2618667E3DD340674EDF15F325B18BD250236E50223AF3915F27711DE7`.

G10-B P3a est fermé hors-jeu sans manifeste : `DataTables+0x1270` conserve le
linker `Stat`; les fonctions gouvernées `0xA12400`/`0xA12420` livrent le count et
chaque nom dans l'ordre physique. ISC12 copie immédiatement ces noms et les
records compilés dans un snapshot propriétaire, calcule puis publie une seule
empreinte avant toute mutation `DescFunc`; un reload identique est accepté et
toute divergence échoue fermée.

G10-B P3b est fermé hors-jeu : les seams uniques `0x9FC654` et `0x9F95A2`
rejoignent la politique d'enveloppe, la transaction atomique, les continuations
natives de cleanup/commit et des relais RX persistants avec état RW/rundown. Le
reader vérifie le statut I/O courant et l'égalité longueur annoncée/réelle avant
copie; le writer borne la taille physique au DWORD natif et ne touche le chemin
final qu'à travers le remplacement atomique. Les hooks restent matériellement
non publiables (`InstalledHookCount == 0`, `codecReady == false`). Les métadonnées
unwind couvrent les allocations propres aux stubs, sans prétendre permettre un
unwind récupérable à travers une entrée tail-jump; les callbacks sont `noexcept`
et toute exception inattendue est terminale.

L'ancien plan canonique G1–G4 testé hors runtime comptait 20 sites mutables et
49 slots, mais l'audit borné du writer G1 en invalide le site étroit
`0x37F186` : l'indexation native d'une table de suppression de 511 DWORD sort de
cette table dès l'ID 511. La source intègre maintenant la correction in-place
des 42 octets exacts `[0x37F17C,0x37F1A6)`. Elle conserve le chemin
compound pour `0..510`, le contourne pour les IDs supérieurs, borne l'émission
à `0xFFF` sur 12 bits et laisse le CALL `0x37F1A1` inchangé. Elle supersède
l'ancien site sans double ownership. Le seed reader `previousStatId = -1` et
le thunk gouverné `0xA1B6C0` restent inchangés. Ce sous-jalon G1–G4 recalculé
comptait 20 sites mutables, 84 slots différents et 71 témoins. Le préflight
complet, les tests de sémantique et de fingerprint, le build Release `/W4 /WX`,
CTest `4/4` et l'unicité de chaque nouvelle signature passent. G1 est donc fermé
en source et statiquement, mais zéro mutation native est publiée. Le guard G3 utilise un leaf
position-independent dans la page RX persistante : il reproduit le used-end
natif dans RAX et livre le flag sticky d'overflow dans EDX. Le CALL rel32 lié
exclusivement par le loader est écrit et flushé avant que le site non
chevauchant `mov eax, edx` soit publié et flushé strictement en dernier. La
padding `0x5353F0` appartient au PDATA/unwind de la fonction suivante et n'est
jamais utilisée comme cave.
Le snapshot clean-sheet refuse `CsvBits > 32` ou `CsvParamBits > 16`, et G2
tient alors à 3 844 octets au pire dans son buffer fixe `0x4000`. Les trois
CALLs exhaustifs G2/G3 sont préparés vers des entries RX process-lifetime et
des FRAME wrappers : v105 exacte, préflight sous lock partagé du snapshot,
appel unique des owners natifs intacts, retour `0x12` sur rejet et postcheck du
cursor sur succès. G4 retargete seulement le CALL de copie preview : l'owner
partagé copie/déverrouille une fois, puis le helper valide tout le D2S v105,
contexte `<4`, marker exigé par le wrapper, IDs/champs/cap/sentinelle; zéro
rejoint l'exit natif qui libère le buffer et retourne false. La branche A legacy
reste 9 bits. Tous ces relais sont préparés mais `operational`/`codecReady`
restent faux et aucun caller production de Commit n'existe.

L'ancien booléen de quiescence a été remplacé par une capacité opaque RAII
`NativePublicationQuiescenceLease`, move-only, dont la validité couvre chaque
fingerprint, write et flush de la
transaction canonique. Les tests prouvent le refus sans lease, la révocation
avant toute écriture et l'obligation de cold restart si l'autorité disparaît
après la première tentative de mutation. Le SDK D2RLoader épinglé ne fournit
toutefois aucune transaction de quiescence ni aucun émetteur de ce lease. Le
code de production ne peut donc pas appeler le commit : il n'existe aucun
issuer, aucun caller et `PublishedCodecMutationCount` reste à zéro. La même
capacité garde maintenant G0; avec le SDK courant, `enabled=true` refuse avant
le mutex, la préparation et toute écriture native.

Le lot source du 31 août ajoute un coordinateur global one-shot, compilé dans
la DLL mais sans caller production ni issuer local. Il impose le preflight G0,
G10, codec avant toute réservation ou écriture, réserve la durée de vie
processus une seule fois, commit G0 puis G10 puis codec, et ne publie la
readiness qu'après les trois succès. Le domaine codec conserve son ordre interne
G9, G2, G4, G1, G3. Une incertitude ou une perte d'autorité post-write rend
`Poisoned` terminal, appelle une seule fois le callback d'état fatal et ne tente
aucun rollback. La lease est revérifiée après la readiness finale; une perte à
cette frontière repasse par poison et retire la publication avant toute reprise.
Le CTest dédié couvre lease absente/révoquée, les trois rejets
de preflight, réentrance, mutate-then-uncertain, états terminaux monotones et
move/release unique. Cette machine d'état ne prétend ni fractionner ni appeler
les publishers réels : les adapters G0/G10/codec restent à séparer et lier
après obtention de l'autorité loader-owned.

L'audit du 31 août ferme cette absence comme fait, pas comme simple inconnue :
le PluginSDK v3 `4933e2c42cb2592958cd0df3b6dc5003102252d1` expose les services
1..15 seulement; Lifecycle et ThreadService donnent un moment/thread de callback,
jamais l'exclusion de tous les consumers D2R. Une future API acceptable doit
être loader-owned, synchrone et non évadable, lier le token au publisher et à
la phase startup, sérialiser tous les publishers et traiter toute incertitude
post-write comme `Poisoned` avant de relâcher la quiescence. Ni un mutex local,
ni une suspension de threads implantée par ISC12, ni l'appel `LoadPlugin` seul
ne satisfait ce contrat.

La proposition gouvernée dans `sdk-contribution/Services-requests.md` fixe le
minimum upstream sous un `ServiceId::NativePublication = 16` provisoire :
callback synchrone, lease owner/thread/epoch-bound, résultats
`Committed/Rejected/Poisoned`, sérialisation de tous les publishers et absence
de reprise après poison. Ce document ne réserve pas officiellement l'ID et ne
remplace aucune implémentation Core; aucun source D2RLoader/Core public ou local
ne permet d'implanter honnêtement cette autorité dans le workspace.

L'audit statique G9 prouve les caps par nœud, les flags de récursion zéro, le
retour zéro sur overflow, les hypothèses consumers et le walker qui émet chaque
enfant en `0x9D`. Douze témoins exacts couvrent maintenant ces surfaces, les
séquences scindées autour des deux CALLs mutables, la vraie queue `0x4817F0` et
son span dispatch. Le planner pur applique 244 octets au root `0x9C` et 239 à
chaque `0x9D`, puis valide tout un snapshot immuable avant le premier callback.
Il rejette les cardinalités incohérentes, les listes d'enfants trouées ou
chevauchées, les indices invalides, les enfants partagés, les cycles, les nœuds
inaccessibles et tout payload hors budget. Son ordre est le preorder depth-first
natif. Il distingue aussi `occupied` de la capacité de sockets afin d'accepter
les items partiellement socketés.

La preuve native ferme `occupied <= 7` par item : la boucle `0x37D60B` compte
les enfants immédiats, clamp à sept puis écrit trois bits à
`0x37D66F..0x37D678`, séparément de `STAT_ITEM_NUMSOCKETS`. Le walker `0x481B50`
et la récursion 9D `0x47A014` ne montrent aucun compteur global. ISC12 n'en
déduit donc aucune borne native totale; son staging thread-local impose ses
propres caps fixes : 64 paquets, `0x4000` octets capturés, profondeur 16 et sept
enfants immédiats par nœud.

La même preuve borne structurellement chaque nœud à sept StatLists/sentinelles
au plus — base, cinq slots set et runeword — et chaque snapshot de liste à 511
records. Six primaires compound de bas ID suppriment huit records partenaires.
Le plafond de 3 577 tokens ID plus sept sentinelles ne décrit ni un item
réalisable ni un worst-case en octets. G9-A copie donc les vrais octets produits
par l'unique sérialisation native et valide le batch complet avant toute queue;
aucune estimation de records n'autorise une publication.

Vincent retient le 30 août 2026 **G9-A native-only** : aucun transport externe
n'appartient au produit. La source prépare désormais le cut-set de quatre sites
comme premier groupe canonique, dans l'ordre exact queue 9C `0x479E10`, queue 9D
`0x47A001`, entry 9C `0x479CD0`, entry 9D `0x479EA0`. Les longs témoins sont
scindés autour des CALLs cinq octets : 9C en `0x479D85+139` et
`0x479E15+14`, 9D en `0x479F76+139` et `0x47A006+19`.

Les relays copient les paquets stack dans le staging TLS sans appeler la vraie
queue. Après le retour du root, le wrapper valide en bloc headers, longueurs,
preorder, parents, cycles et les quatre caps; un rejet abort/discard avant tout
envoi, une acceptation rejoue via la queue native inchangée `0x4817F0`. Les
entries utilisent des trampolines enregistrés de 10 octets. Leur unwind vivant
est enregistré avec `RtlAddFunctionTable` avant toute tentative de publication;
un échec est fail-closed. Deux nouveaux témoins d'épilogue exacts et uniques,
`[0x479E23,0x479E41)` et `[0x47A019,0x47A037)`, couvrent cookie check,
désallocation de stack, pops nonvolatiles et `RET`. Le loader source exige aussi
que `RtlLookupFunctionEntry`, interrogé au corps et au `RET`, retourne les mêmes
Begin/End/UnwindData, avec End dans l'image et une plage couvrant l'épilogue.
Le `.pdata` statique réhydraté est protégé et non autoritatif : ces checks live
ne sont pas attestés et peuvent refuser le runtime officiel au prochain gate.
Les sorties producteur anormales passent par un `finally` SEH qui abort/discard
la transaction.

G9-A est donc fermé en source et statiquement, pas en runtime. Le plan canonique
complet possède maintenant cinq groupes, 24 sites mutables, 102 mutations et 77
témoins, tous préflightés et tous non publiés. Il n'existe aucun
caller production de `CommitPreparedCodecPatchSet`; `itemTransportReady == 0`,
`codecReady == 0` et le compteur publié reste zéro.

`G0-BBE` a maintenant son census natif et dix fingerprints proof-only. La
largeur du resolver, du token 5 et du decoder est prouvée jusqu'à `4094`; la
capture read-only du runtime officiel atteste aussi le littéral `stat` du slot
5 et le record callback `0x3B33F0/arity 2`. Le membership natif est fermé. Il
demeure distinct de la fonctionnalité officielle eezstreet
`items.playerConditionCalc` et ne ferme aucune autorité de publication.

Le build Release gouverné passe `/W4 /WX` et CTest `4/4`; la DLL vaut
`FF8D16AF4A6DBCB9BD3AD86A6A6DFCBB4553D26A200DF161B1065C6A5DFE5286`. Le
ledger passe `VALID 211/15` et l'unicité
des signatures hors runtime. Le plan courant reste à 24 sites mutables, 102
mutations, 77 témoins et zéro publication. Aucun déploiement, lancement D2R par
ISC12, fichier de sauvegarde ou test multijoueur n'a été touché; la seule
opération live est l'attestation externe read-only décrite ci-dessus.

Le prochain blocker est externe et architectural : obtenir de D2RLoader une
transaction synchrone documentée capable d'émettre le lease production, de
sérialiser les publishers et d'empêcher toute reprise sur résultat empoisonné.
Le SDK v3 épinglé n'expose aucun tel service; `D2RLoaderLoadPlugin` n'est pas une
preuve de quiescence. Le coordinateur full-set est maintenant source-préparé et
testé; le prochain lot local doit fractionner G0, G10 et codec en preflight et
commit immuables puis les lier sans créer de caller production. Le raccordement
réel attend le service loader-owned. Les memberships protégés G0-BBE sont
fermés read-only. Seulement après l'autorité, les adapters et le caller
transactionnel viendront l'unwind live, le watcher BBE pré/post-TXT, le cold
start puis les cas 0x9C/0x9D, overflow, réentrance, backpressure, coexistence,
save/reload et multijoueur. **La publication runtime ISC12, les sauvegardes et
le multijoueur restent NOT RUN.**
