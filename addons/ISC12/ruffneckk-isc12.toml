config_version = 1
# ISC12 0.2.0 remains non-operational. The pinned loader exposes no native
# publication-quiescence authority, so enabled=true is refused with zero writes.
enabled = false

[diagnostics]
# Reserved for guarded diagnostics after a loader authority becomes available.
enabled = false
