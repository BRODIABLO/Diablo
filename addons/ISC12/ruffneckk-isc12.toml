config_version = 1
# ISC12 0.2.0 contains only the guarded loader extension. Keep it disabled
# until the clean-sheet marker, save codecs and network gates are complete.
enabled = false

[diagnostics]
# Logs the guarded loader diagnostics when the experimental stage is enabled.
enabled = false
